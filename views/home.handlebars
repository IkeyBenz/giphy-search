<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>Example App</title>
        <link rel="stylesheet" href="styles.css">
    </head>
    <body>
        <header>
            <h1>Search for GIFs</h1>
            <form action="/">
                <input type="text" name="term">
                <button type="submit">Search</button>
            </form>
        </header>
        <main>
            {{#each gifs}}
                <a href="{{this.images.fixed_height.url}}"><img src="{{this.images.fixed_height.url}}"></a>
            {{/each}}
        </main>
        
    </body>
</html>